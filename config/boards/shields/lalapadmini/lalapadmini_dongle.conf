# LalaPadmini Dongle Configuration - Disable Studio to prevent physical layout conflicts
# CONFIG_ZMK_STUDIO is not set

# Disable trackpad and rotary encoder for dongle mode
CONFIG_INPUT=n
CONFIG_SPI=n
CONFIG_ZMK_POINTING=n

# Split keyboard settings  
CONFIG_ZMK_SPLIT_BLE=y